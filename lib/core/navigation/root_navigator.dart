import 'package:flutter/material.dart';

/// Global navigator for routing from notification taps before a `BuildContext` exists.
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();
